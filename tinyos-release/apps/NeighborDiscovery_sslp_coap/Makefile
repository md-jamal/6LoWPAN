COMPONENT=TestAppC

include $(MAKERULES)
PFLAGS += -DNODE_LBR
#PFLAGS += -DNODE_ROUTER
#PFLAGS += -DNODE_HOST


#CFLAGS+=-DPRINTFUART_ENABLED

#CFLAGS+=-DNEW_PRINTF_SEMANTICS
#CFLAGS += -DPRINTF_BUFFER_SIZE=700
CFLAGS+=-DRPL_ROUTING -DRPL_STORING_MODE -I$(LOWPAN_ROOT)/tos/lib/net/rpl
PFLAGS+=-DIN6_PREFIX=\"3000::\"
PFLAGS+=-DGLOBAL_PREFIX=\"4000::\"

CFLAGS += -DCOAP_SERVER_PORT=61616L
